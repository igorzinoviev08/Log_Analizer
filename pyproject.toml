[tool.poetry]
name = "pythonproject"
version = "0.1.0"
description = ""
authors = ["igor <zinovievigor@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "-v --cov=log_analyzer --cov-report=term-missing"

# 🌐 Ссылка для клонирования:

https://github.com/igorzinoviev08/django-log-analyzer
